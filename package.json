{
  "name": "image-masher",
  "author": "Zack Creach",
  "version": "1.0.1",
  "description": "Image masher – Image compression tool using imagemin & gulp",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf /replace && gulp copy && gulp replace && gulp insert && open replace/"
  },
  "license": "ISC",
  "devDependencies": {
    "del": "3.0.0",
    "gulp": "3.9.1",
    "gulp-replace": "^0.6.1",
    "open": "0.0.5"
  }
}
